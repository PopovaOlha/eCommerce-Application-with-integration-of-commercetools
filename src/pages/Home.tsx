import { observer } from 'mobx-react'
import Footer from '../components/Footer'

function Home() {
  return (
    <div>
      {/* <Header /> */}
      <Footer />
    </div>
  )
}

export default observer(Home)
